#apiVersion: networking.k8s.io/v1
#kind: Ingress
#metadata:
#  name: "{{ .Values.APP_NAME }}"
#  annotations:
#    ingress.kubernetes.io/ssl-redirect: "false"
#    traefik.ingress.kubernetes.io/router.entrypoints: web
#spec:
#  rules:
#    - host: "api.{{ .Release.Namespace }}"
#      http:
#        paths:
#          - path: "/{{ .Values.APP_NAME }}"
#            pathType: Prefix
#            backend:
#              service:
#                name: "{{ .Values.APP_NAME }}"
#                port:
#                  number: 8080
---
#apiVersion: networking.k8s.io/v1
#kind: Ingress
#metadata:
#  name: "{{ .Values.APP_NAME }}-external"
#  annotations:
#    ingress.kubernetes.io/ssl-redirect: "false"
#    traefik.ingress.kubernetes.io/router.entrypoints: web
#spec:
#  rules:
#    - host: "api.{{ .Values.ITM_ADVICE_HOST }}"
#      http:
#        paths:
#          - path: "/{{ .Values.APP_NAME }}"
#            pathType: Prefix
#            backend:
#              service:
#                name: "{{ .Values.APP_NAME }}"
#                port:
#                  number: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: "{{ .Values.APP_NAME }}-nodeport"
  namespace: "{{ .Release.Namespace }}"
spec:
  type: NodePort
  selector:
    app: "{{ .Values.APP_NAME }}"
  ports:
    - port: 8080           # Внутренний порт сервиса
      targetPort: 8080     # Порт контейнера
      nodePort: 32106      # Внешний порт на ноде (в диапазоне 30000–32767)
