apiVersion: v1
kind: Service
metadata:
  name: "{{ .Values.APP_NAME }}"
  labels:
    app: "{{ .Values.APP_NAME }}"

spec:
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: "{{ .Values.APP_NAME }}"